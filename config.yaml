server:
  port: 3000
  workspaceDir: ./workspace

repositories:
  - name: demo-webapp
    url: https://github.com/pepavlin/demo-webapp.git
    defaultBranch: main
    allowedTools:
      - Bash
      - Read
      - Write
      - Edit
      - Glob
      - Grep

claudeCode:
  command: claude
  model: sonnet
  dockerImage: implementer-sandbox
